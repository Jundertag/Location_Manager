package com.jayden.locationmanager.data.model

enum class FaaModeIndicator {
    AUTONOMOUS_MODE,
    CAUTION,
    DIFFERENTIAL_MODE,
    ESTIMATED_MODE,
    RTK_FLOAT_MODE,
    MANUAL_INPUT_MODE,
    NOT_VALID,
    PRECISE,
    RTK_INTEGER_MODE,
    SIMULATOR_MODE,
    UNSAFE
}